import { ComingSoon } from "./ComingSoon";

export function LearnScreen() {
  return (
    <ComingSoon
      emoji="🌱"
      title="Learn"
      note="Short lessons are on the way — one new idea at a time."
    />
  );
}
